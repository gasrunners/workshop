{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": [".env"],
  "globalEnv": ["INFURA_KEY"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "$NODE_ENV", "$VERCEL_URL", "$PORT"],
      "env": ["NODE_ENV", "VERCEL_URL", "PORT"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "dependsOn": ["$NODE_ENV", "$VERCEL_URL", "$PORT"],
      "env": ["NODE_ENV", "VERCEL_URL", "PORT"],
      "outputs": []
    },
    "lint:fix": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "test": {
      "outputs": []
    },
    "test:watch": {
      "cache": false
    },
    "test:coverage": {
      "outputs": []
    }
  }
}
